# 模式 G：对标拆解与策略内化 (Benchmarking & Scaling)

**定位**：数字分身的”博主实验室”。逆向解剖顶级内容，提取可以直接给模式 C 调用的创作框架。

## ⛔ 执行前强制检查 (Pre-flight Checklist)

**在执行任何生成动作前，必须逐项确认以下检查点。如果跳过任何 REQUIRED 步骤，视为违规操作。**

```
模式 G 执行检查清单：
□ Step 1: 情报获取与身份判别
  □ 已获取内容（WebFetch 或用户粘贴）
  □ 已识别作者身份（自己 → 模式 E / 他人 → 继续）

□ Step 2: 内容三层逆向解剖
  □ 已完成 L1 标题与钩子策略分析
  □ 已完成 L2 逻辑框架逆向
  □ 已完成 L3 独特技巧提取

□ Step 3: 兼容性与人设冲突评分
  □ 已评估语气兼容度
  □ 已评估价值观风险
  □ 已给出策略应用建议

□ Step 4: 策略资产化存储 ⚠️ REQUIRED
  □ 已生成策略文件存入 assets/strategies/
  □ 已更新 system/profile.json 的技能树

□ Step 5: 后续操作询问 ⚠️ REQUIRED - 必须使用 AskUserQuestion
  □ 已显示拆解结果摘要
  □ 已推荐后续操作
  □ 已调用 AskUserQuestion 询问后续操作
```

---

## Step 1: 情报获取与”身份判别” (Ingestion & Routing)

1. **内容读取**：使用 WebFetch 抓取链接或直接读取用户粘贴的文本。
2. **身份识别**：系统识别内容作者。
   - **如果是你自己写的**：自动跳转 **模式 E (加权风格学习)**。
   - **如果是别人写的**：继续执行模式 G 的解剖流程。

---

## Step 2: 内容三层逆向解剖 (Reverse Engineering)

系统对外部文章执行”剥洋葱”式的深度拆解：

### L1：标题与钩子策略 (Hook Analysis)
- **分析**：其标题逻辑是什么（悬念/利益/反直觉）？
- **分析**：前 3 句（Hooks）是如何留住读者的？

### L2：逻辑框架逆向 (Skeleton Analysis) - **核心步骤**
- **逻辑**：逆向出其全篇的大纲结构。
- **示例**：[痛点挖掘] -> [反直觉事实] -> [深度原理] -> [行动清单] -> [情绪反转]。

### L3：独特技巧提取 (Technique Analysis)
- **分析**：其独特的叙事技巧、比喻方式或数据呈现法。

---

## Step 3: 兼容性与人设冲突评分 (Stance & Compatibility Check)

系统对比你的 `persona/` 和 `memory/` 进行安全评估：

1. **语气兼容度**：该风格是否适合你的 `voice_style.json`？
2. **价值观风险**：其核心主张是否违背了你的 `stance_topics.json`？
3. **策略应用建议**：
   - *”框架非常优秀，建议完整套用。”*
   - *”该作者语气过于激进，建议学习其逻辑但用你自己的温和语气重构。”*

---

## Step 4: 策略资产化存储 (Strategy Capture) ⚠️ REQUIRED

**必须使用 Write 工具执行以下操作**：

1. **生成策略文件**：将拆解出的结构化框架生成 `.md` 文件。
   - **路径**：`./.creator-space/assets/strategies/learned_[博主名]_[主题].md`
   - **格式**：
     ```markdown
     # [策略名称]

     ## 来源
     - 博主：[博主名]
     - 原文：[链接]
     - 拆解日期：[日期]

     ## 标题逻辑
     [分析结果]

     ## 内容框架
     [逆向的大纲结构]

     ## 独特技巧
     [提取的技巧]

     ## 兼容性评估
     - 语气兼容度：[高/中/低]
     - 价值观风险：[无/低/高]
     - 应用建议：[建议]

     ## 模式 C 调用模板
     [可直接使用的模板]
     ```
2. **更新技能树**：在 `system/profile.json` 的 `learned_techniques` 字段中记录并打标。

---

## Step 5: 后续操作询问 ⚠️ REQUIRED

**完成后，必须使用 AskUserQuestion 询问后续操作**：

```markdown
✅ 对标拆解完成

📊 本次拆解概览：
- 博主：[博主名]
- 标题逻辑：[类型]
- 内容框架：[框架名称]
- 独特技巧：[X] 个
- 兼容度：[高/中/低]

📁 策略已保存：
- assets/strategies/learned_[博主名]_[主题].md

🔄 接下来你可以：

- [A] 用这个策略创作 → 进入模式 C，应用新策略
- [B] 学习这个博主的风格 → 切换到模式 E 进行深度风格学习
- [C] 拆解更多博主 → 继续模式 G
- [D] 查看策略库 → 显示 assets/strategies/ 下所有策略
- [E] 结束，稍后处理

请选择后续操作：
```

### 模式跳转映射表

| 用户选择 | 跳转模式 | 执行动作 |
|---------|---------|---------|
| A | 模式 C | 携带新策略进入平台写作 |
| B | 模式 E | 深度学习该博主风格 |
| C | 继续模式 G | 拆解下一个博主 |
| D | - | 显示策略库目录 |
| E | 结束 | 保存后退出 |

### AskUserQuestion 配置示例

```json
{
  “questions”: [
    {
      “question”: “策略已入库，接下来你想做什么？”,
      “header”: “后续操作”,
      “multiSelect”: false,
      “options”: [
        {
          “label”: “用这个策略创作”,
          “description”: “进入模式 C，立即应用新策略写一篇内容”
        },
        {
          “label”: “学习这个博主的风格”,
          “description”: “进入模式 E，深度学习其语气和表达方式”
        },
        {
          “label”: “拆解更多博主”,
          “description”: “继续模式 G，拆解下一个博主的方法论”
        },
        {
          “label”: “查看策略库”,
          “description”: “显示 assets/strategies/ 下所有可用策略”
        }
      ]
    }
  ]
}
```
